// function subString(text, start, count) {
//     let result;
//     result = text.slice(start, start + count);
//     console.log(result);
// }


function subString(text, start, count) {
    let result;
    result = text.substring(start, start + count);
    console.log(result);
}

subString('ASentence', 2, 8)
subString('SkipWord', 4, 7)
